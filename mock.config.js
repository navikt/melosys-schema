const PROJECT_HOME_DIR = `${process.cwd()}`;
const SCRIPTS_DIR = `${PROJECT_HOME_DIR}/scripts`;
const LOGDIR = `${PROJECT_HOME_DIR}/logdir`;
const MOCK_DATA_DIR = `${PROJECT_HOME_DIR}/mock_data`;
const SCHEMA_DIR = `${PROJECT_HOME_DIR}/schema`;
const DEFINITION_SCHEMA = `${SCHEMA_DIR}/definitions-schema.json`;

module.exports = {
  PROJECT_HOME_DIR,
  SCRIPTS_DIR,
  MOCK_DATA_DIR,
  SCHEMA_DIR,
  LOGDIR,
  DEFINITION_SCHEMA,
};

