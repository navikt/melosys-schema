const { journalforing} = require('./journalforing');
const { opprett} = require('./opprett');
const { sed} = require('./sed');
const { tilordne } = require('./tilordne');

module.exports = {
  journalforing,
  opprett,
  sed,
  tilordne,
};
